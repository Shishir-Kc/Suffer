{
  "name": "Backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:migrate:local": "wrangler d1 migrations apply suffer-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply suffer-db --remote"
  },
  "dependencies": {
    "hono": "^4.13.7",
    "jose": "^6.2.12",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "wrangler": "4.137.0"
  }
}
